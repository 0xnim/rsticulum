[package]
name = "rsticulum-crypto"
version.workspace = true
edition.workspace = true
description = "Cryptographic primitives for rsticulum — HKDF-SHA256 key derivation and RNS Token (simplified Fernet)"
license.workspace = true

[dependencies]
hmac = "0.12"
sha2.workspace = true
rand.workspace = true
aes = "0.8"
cbc = "0.1"
thiserror = "2"

[dev-dependencies]
hex = "0.4"
