//! ContentStore — LRU cache of Data packets.
// Stub — implemented in Phase 2

pub struct ContentStore {
    _private: (),
}

impl ContentStore {
    #[allow(clippy::new_without_default)]
    pub fn new(_max_entries: usize) -> Self {
        ContentStore { _private: () }
    }
}
